/** Blanc editorial card shell for planning chapters. */
export const couplePlanningSectionCardClass =
  "!border-stone-200/45 !bg-[#fcfbf8] !shadow-[0_2px_14px_-6px_rgba(28,25,23,0.08)] !p-6 sm:!p-8 md:!p-9";

/** Single conversational prompt — one question per surface. */
export const couplePlanningQuestionShellClass =
  "rounded-2xl border border-stone-200/50 bg-white/60 px-5 py-6 sm:px-7 sm:py-7";

/** Conversational question copy (not admin form labels). */
export const couplePlanningQuestionLabelClass =
  "text-base font-medium leading-snug tracking-normal text-stone-900 sm:text-[1.0625rem]";

/** Supporting helper under a question. */
export const couplePlanningQuestionHelperClass =
  "text-sm leading-relaxed text-stone-600";

/** Muted empty / placeholder answers. */
export const couplePlanningEmptyAnswerClass = "text-sm text-stone-500";

/** Section eyebrow — matches dashboard editorial rhythm. */
export const couplePlanningEyebrowClass =
  "text-[11px] font-semibold uppercase tracking-[0.18em] text-stone-600";

/** Chapter title — editorial serif. */
export const couplePlanningTitleClass =
  "mt-3 font-[family-name:var(--font-editorial),Iowan_Old_Style,Palatino_Linotype,Georgia,serif] text-xl font-medium leading-snug tracking-tight text-stone-900 sm:text-[1.625rem]";

/** Chapter intro paragraph. */
export const couplePlanningIntroClass =
  "mt-3 max-w-prose text-[15px] leading-relaxed text-stone-700 sm:text-base";

/** Native select inside couple planning prompts. */
export const couplePlanningSelectClass =
  "cm-select-light mt-3 box-border block w-full min-w-0 max-w-full min-h-12 cursor-pointer touch-manipulation appearance-none rounded-xl border border-stone-300/90 bg-white py-3.5 pl-3 pr-10 text-left text-base leading-snug text-stone-900 shadow-sm transition-colors focus:border-[#2f4a3e]/50 focus:outline-none focus:ring-2 focus:ring-[#2f4a3e]/25 focus:ring-offset-0 disabled:cursor-not-allowed disabled:opacity-60";

/** Inline prompt card (lineup, toasts, etc.). */
export const couplePlanningPromptCardClass =
  "rounded-2xl border border-stone-200/50 bg-white/70 px-5 py-6 sm:px-7 sm:py-7";
